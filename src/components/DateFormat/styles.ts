import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  title: {
    color: '#0E0E11',
    fontSize: 32,
    marginBottom: 8,
    fontWeight: '700',
  },
});

export default styles;
